package sorting.bucket;

public class BucketSort {

    /**
     * Bucket Sort
     *
     *
     *
     * Worst case: O(n*n) when all elements at allocated to the same bucket.
     *
     * @param arr
     * @param maxValue
     */
    public static void bucketSort(int[] arr, int maxValue) {

    }
}
